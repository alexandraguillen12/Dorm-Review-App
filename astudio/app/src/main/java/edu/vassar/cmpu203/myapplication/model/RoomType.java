package edu.vassar.cmpu203.myapplication.model;

/**
 * The enum RoomType representing different type of the rooms within a context
 * representing a set of named for specified room types and a constant for all rooms.
 */
public enum RoomType {
    SINGLE,
    DOUBLE,
    TRIPLE,
    QUAD,
    SUITE,
    TWO_ROOM_DOUBLE,
    TWO_ROOM_TRIPLE,
    ALL_ROOM_TYPES
}
